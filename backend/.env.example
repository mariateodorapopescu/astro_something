# Copiaza acest fisier in "backend/.env" si pune valorile tale reale.
# Fisierul .env NU se urca in git (e in .gitignore).

DATABASE_URL=postgresql://app_user:app_password@localhost:5432/app_db
CORS_ORIGINS=http://localhost:4200
